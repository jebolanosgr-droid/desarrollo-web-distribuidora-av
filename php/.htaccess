Options -Indexes

<FilesMatch "^(config|database|auth|csrf|functions)\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

<IfModule mod_alias.c>
    RedirectMatch 403 ^/(includes|sql)(/|$)
</IfModule>
